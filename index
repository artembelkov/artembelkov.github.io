<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Your Name | Portfolio</title>

  <!-- Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --bg-main: #f8fafc;
      --glass: rgba(255, 255, 255, 0.65);
      --border: rgba(15, 23, 42, 0.08);
      --text-main: #0f172a;
      --text-muted: #475569;
      --accent1: #38bdf8;
      --accent2: #818cf8;
      --accent3: #22d3ee;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Inter", sans-serif;
    }

    body {
      min-height: 100vh;
      background:
      radial-gradient(600px at 20% 20%, rgba(56,189,248,0.45), transparent),
      radial-gradient(600px at 80% 30%, rgba(129,140,248,0.45), transparent),
      radial-gradient(500px at 50% 80%, rgba(34,211,238,0.35), transparent),
      #f8fafc;
      color: #0f172a;
    }


    a {
      color: #2563eb;
      text-decoration: none;
    }

    .container {
      max-width: 1120px;
      margin: auto;
      padding: 100px 24px;
    }

    /* HERO */
    header {
      margin-bottom: 120px;
    }

    header h1 {
      font-size: clamp(2.8rem, 6vw, 3.8rem);
      font-weight: 700;
      background: linear-gradient(135deg, var(--accent1), var(--accent2));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 20px;
    }

    header p {
      max-width: 720px;
      font-size: 1.15rem;
      color: var(--text-muted);
    }

    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 32px;
    }

    .tag {
      padding: 7px 16px;
      font-size: 0.75rem;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      border-radius: 999px;
      background: linear-gradient(
        135deg,
        rgba(56,189,248,0.25),
        rgba(129,140,248,0.25)
      );
      border: 1px solid var(--border);
      backdrop-filter: blur(8px);
    }

    /* SECTIONS */
    section {
      margin-bottom: 120px;
    }

    section h2 {
      font-size: 1.7rem;
      font-weight: 600;
      margin-bottom: 40px;
      position: relative;
    }

    section h2::after {
      content: "";
      width: 46px;
      height: 3px;
      background: linear-gradient(90deg, var(--accent1), var(--accent2));
      position: absolute;
      left: 0;
      bottom: -14px;
      border-radius: 999px;
    }

    /* CARDS */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 34px;
    }

    .card {
  position: relative;
  border-radius: 24px;
  padding: 0; /* remove padding from blurred container */
  overflow: hidden;

  /* glass background */
  background: rgba(255, 255, 255, 0.35);
  border: 1px solid rgba(255, 255, 255, 0.5);
  backdrop-filter: blur(28px) saturate(180%);
  -webkit-backdrop-filter: blur(28px) saturate(180%);
  box-shadow: 0 30px 60px rgba(15, 23, 42, 0.18),
              inset 0 1px 0 rgba(255, 255, 255, 0.7);
  transition: transform 0.35s ease, box-shadow 0.35s ease;
}

/* Highlight / reflection */
.card::before {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(
    120deg,
    rgba(255, 255, 255, 0.6),
    rgba(255, 255, 255, 0.15) 35%,
    transparent 60%
  );
  pointer-events: none;
}

/* Inner content: text stays sharp */
.card-content {
  position: relative;
  padding: 36px;
  z-index: 1;
  color: #0f172a; /* text color for light mode */
}

    .card:hover {
  transform: translateY(-10px);
  box-shadow:
    0 50px 100px rgba(15, 23, 42, 0.22),
    inset 0 1px 0 rgba(255, 255, 255, 0.75);
}


    .card h3 {
      font-size: 1.15rem;
      font-weight: 600;
      margin-bottom: 12px;
    }

    .card p {
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    /* STACK (for Education etc.) */
    .stack {
      display: grid;
      gap: 32px;
    }

    /* FOOTER */
    footer {
      text-align: center;
      font-size: 0.9rem;
      color: var(--text-muted);
    }

    footer a {
      margin: 0 12px;
    }

    @media (max-width: 600px) {
      .container {
        padding: 80px 20px;
      }
    }
  </style>
</head>

<body>
  <div class="container">

    <!-- HERO -->
    <header>
      <h1>Your Name</h1>
      <p>
        Business Administration student focused on finance, accounting,
        and data-driven decision making.
      </p>

      <div class="tags">
        <div class="tag">Finance</div>
        <div class="tag">Accounting</div>
        <div class="tag">Data Analysis</div>
        <div class="tag">Management</div>
      </div>
    </header>

<section>
  <h2>Projects</h2>

  <div class="grid">
    <!-- Project 1 -->
    <div class="card">
      <div class="card-content">
        <h3>AML in European Banking</h3>
        <p>
          Research project analyzing anti-money laundering mechanisms
          in the European banking sector. Focused on risk assessment
          and compliance frameworks.
        </p>
        <a href="#">View project →</a>
      </div>
    </div>

    <!-- Project 2 -->
    <div class="card">
      <div class="card-content">
        <h3>Financial Analysis Toolkit</h3>
        <p>
          Excel-based models for financial statement analysis,
          forecasting, and valuation.
        </p>
        <a href="#">View project →</a>
      </div>
    </div>

    <!-- Project 3 -->
    <div class="card">
      <div class="card-content">
        <h3>Data Analytics Practice</h3>
        <p>
          Practical analytics exercises using real-world
          business datasets to derive actionable insights.
        </p>
        <a href="#">View project →</a>
      </div>
    </div>
  </div>
</section>

<section>
  <h2>Experience</h2>

  <div class="stack">
    <!-- Experience 1 -->
    <div class="card">
      <div class="card-content">
        <h3>Senior Manager — Online English School</h3>
        <p><strong>2023 – Present</strong></p>
        <p>
          Managed CRM, HR, and customer support operations. Improved
          retention and achieved an 88% customer satisfaction score.
        </p>
      </div>
    </div>

    <!-- Experience 2 -->
    <div class="card">
      <div class="card-content">
        <h3>Founder — Youth English Club (NGO)</h3>
        <p><strong>2022 – Present</strong></p>
        <p>
          Founded and led an international NGO recognized with
          the European Citizen’s Prize for social impact.
        </p>
      </div>
    </div>
  </div>
</section>


    <!-- EDUCATION -->
    <section>
      <h2>Education</h2>

      <div class="stack">
        <div class="card">
          <div class="card-content">
            <h3>B.A. Business Administration</h3>
            <p>University of Tartu — GPA 4.0 / 4.0</p>
          </div>
        </div>

        <div class="card">
          <div class="card-content">
          <h3>Corporate Finance Certification</h3>
          <p>CFI — Financial Modeling & Valuation Analyst (FMVA®)</p>
          </div>
        </div>
      </div>
    </section>

    <!-- FOOTER -->
    <footer>
      <p>
        your.email@example.com ·
        <a href="https://github.com/yourusername">GitHub</a> ·
        <a href="https://linkedin.com/in/yourusername">LinkedIn</a>
      </p>
    </footer>

  </div>
</body>
</html>
